server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/smart_cs_admin?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_ROOT_PASSWORD:liu317228}
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: redis
      port: 6379

app:
  name: smart-cs-admin

cors:
  allowed-origins: "*"
  allowed-methods: GET,POST,PUT,DELETE
  allowed-headers: "*"

upload:
  path: ./uploads
  max-size: 100MB

logging:
  level:
    com.smartcs: info
