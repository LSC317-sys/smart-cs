# SmartCS 监控 + 日志栈部署指南
# 依据：设计文档 v3.0 第 6 章
# 生成时间：2026-05-23

## ========== 1. 上传配置文件 ==========

# 1.1 上传 docker-compose.yml
scp D:\my-projects\smart-c-s\docker\docker-compose.yml root@121.199.172.24:/opt/smart-c-s/docker/

# 1.2 上传 Prometheus 配置
scp D:\my-projects\smart-c-s\docker\prometheus.yml root@121.199.172.24:/opt/smart-c-s/docker/
scp D:\my-projects\smart-c-s\docker\smartcs_alerts.yml root@121.199.172.24:/opt/smart-c-s/docker/

# 1.3 上传 Loki + Promtail 配置
scp D:\my-projects\smart-c-s\docker\loki-config.yml root@121.199.172.24:/opt/smart-c-s/docker/
scp D:\my-projects\smart-c-s\docker\promtail-config.yml root@121.199.172.24:/opt/smart-c-s/docker\

## ========== 2. 重新构建业务服务 ==========

# 2.1 本地构建所有服务 JAR
cd D:\my-projects\smart-c-s
mvn clean package -DskipTests

# 2.2 上传业务服务 JAR 到服务器
scp smart-c-s-gateway/target/*.jar root@121.199.172.24:/opt/smart-c-s/smart-c-s-gateway/
scp smart-c-s-chat/target/*.jar root@121.199.172.24:/opt/smart-c-s/smart-c-s-chat/
scp smart-c-s-knowledge/target/*.jar root@121.199.172.24:/opt/smart-c-s/smart-c-s-knowledge/
scp smart-c-s-user/target/*.jar root@121.199.172.24:/opt/smart-c-s/smart-c-s-user/
scp smart-c-s-admin/target/*.jar root@121.199.172.24:/opt/smart-c-s/smart-c-s-admin/

## ========== 3. 部署监控 + 日志栈 ==========

# 3.1 SSH 登录服务器
ssh root@121.199.172.24

# 3.2 进入项目目录
cd /opt/smart-c-s/docker

# 3.3 拉取镜像（如果不存在）
docker compose pull prometheus grafana node-exporter cadvisor loki promtail

# 3.4 启动所有服务（包括新的监控 + 日志服务）
docker compose -f docker-compose.yml up -d

# 3.5 查看服务状态
docker compose -f docker-compose.yml ps

# 3.6 查看日志
docker compose -f docker-compose.yml logs -f

## ========== 4. 验证部署 ==========

# 4.1 访问 Prometheus Web UI
http://121.199.172.24:9090

# 4.2 访问 Grafana（用户名/密码：admin/liu317228）
http://121.199.172.24:3000

# 4.3 访问 Loki API
http://121.199.172.24:3100/ready

# 4.4 访问 cAdvisor（容器监控）
http://121.199.172.24:8080

# 4.5 访问 Node Exporter（主机监控）
http://121.199.172.24:9100/metrics

## ========== 5. 配置 Grafana ==========

# 5.1 添加 Prometheus 数据源
# URL: http://prometheus:9090

# 5.2 添加 Loki 数据源
# URL: http://loki:3100

# 5.3 导入 Dashboard
# - JVM Micrometer (ID: 4701)
# - Node Exporter Full (ID: 1860)
# - Docker Container Overview (ID: 11558)

## ========== 6. 执行数据库索引 ==========

# 6.1 上传 SQL 脚本到服务器
scp D:\my-projects\smart-c-s\docs\创建索引.sql root@121.199.172.24:/tmp/

# 6.2 执行 SQL 脚本
ssh root@121.199.172.24 "mysql -u root -pliu317228 smart_cs < /tmp/创建索引.sql"

# 6.3 验证索引创建
ssh root@121.199.172.24 "mysql -u root -pliu317228 smart_cs -e 'SHOW INDEX FROM users;'"

## ========== 注意事项 ==========

1. **服务器资源限制**：2 核 4G 内存，所有 Java 服务已配置 `-Xmx256m`
2. **阿里云试用到期**：约 2026-07-06，需提前迁移或续费
3. **防火墙配置**：确保 3000、9090、3100、9080 端口已开放
3. **Grafana 首次登录**：用户名 `admin`，密码 `liu317228`
4. **Prometheus Targets**：检查所有 target 是否为 `UP` 状态
5. **Loki 日志**：等待 1-2 分钟让 Promtail 采集日志并发送到 Loki

## ========== 故障排查 ==========

# 查看服务日志
docker logs smartcs-prometheus
docker logs smartcs-grafana
docker logs smartcs-loki
docker logs smartcs-promtail

# 进入容器调试
docker exec -it smartcs-prometheus sh
docker exec -it smartcs-loki sh

# 检查网络连接
docker network inspect smart-c-s_docker_smartcs-net
