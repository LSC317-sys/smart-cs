server:
  port: 8084

spring:
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/smart_cs?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: liu317228
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379

app:
  name: smart-cs-admin

# CORS 璺ㄥ煙閰嶇疆
cors:
  allowed-origins: "*"
  allowed-methods: GET,POST,PUT,DELETE
  allowed-headers: "*"

# 鏂囦欢涓婁紶
upload:
  path: ./uploads
  max-size: 100MB

logging.level.com.smartcs: debug
