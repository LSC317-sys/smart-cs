server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/smart_cs_user?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_ROOT_PASSWORD:liu317228}
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: redis
      port: 6379

jwt:
  secret: your-jwt-secret-key-here
  expiration: 86400000

admin:
  invitation-code: SMARTCS_ADMIN_SECRET_2026

logging:
  level:
    com.smartcs: info
